[pytest]
pythonpath = ./
markers =
    perf_cpu: CPU bound benchmark (FakeSession, no real DB)
    perf_db: DB benchmark (real database required)
filterwarnings =
    ignore:.*Repository-level session was provided via __init__.*:UserWarning